# wear-os-music-sync
Simple project to establish music sync from desktop to wear os device.


# Sample calling
$ curl http://<machine-ip-address>/files/all
[<RETURNS>, <ARRAY>, <OF>, <FILES>]
$ curl http://<mdns.local>/files/all
[<RETURNS>, <ARRAY>, <OF>, <FILES>]